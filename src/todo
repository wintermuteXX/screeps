1. TODO
    - Pickup Minerals
    - Check Scout Code
    - Tower Repair + Heal
    - Repair Container
    - Get_energy_container
    - Upgrader Link -> Storage
    - Boost creeps
    -- Sell + Buy at Market
    -- Harvest Power
    -- Harvest Minerals
2. Implement 
3. Code Snippets

Object.defineProperty(Source.prototype, “memory”, {
  configurable: true,
  get() {
    if (_.isUndefined(this.room.memory.sources)) {
      this.room.memory.sources = {};
    }
    return this.room.memory.sources[this.id] = this.room.memory.sources[this.id] || {};
  },
  set(value) {
    if (_.isUndefined(this.room.memory.sources)) {
      this.room.memory.sources = {};
    }
    if (!_.isObject(Memory.structure)) {
      throw new Error(“Could not set structure memory”);
    }
    this.room.memory.sources[this.id] = value;
  },
});


---------------------

if (claimer.pos.isNearTo(this.controller)) {
            claimer.claimController(this.controller);
        }
        else {
            claimer.travelTo(this.controller);
        }
----------------------
room.energyAvailablenumber
Total amount of energy available in all spawns and extensions in the room.

room.energyCapacityAvailablenumber
Total amount of energyCapacity of all spawns and extensions in the room.

